services:
  recrew:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: recrew-v4
    env_file: .env
    volumes:
      - ./reports:/app/reports
    network_mode: host
    command: ${TARGET_URL:-https://example.com}
    restart: "no"
